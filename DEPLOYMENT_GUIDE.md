# 🚀 快速部署與開發指南 (Deployment Guide)

這份文件是為了幫助開發者快速掌握本專案的 **同步 (Sync)** 與 **部署 (Deploy)** 流程。

## 📋 目錄
1. [同步代碼 (Push & Pull)](#1-同步代碼-push--pull)
2. [部署到 Firebase (Deploy)](#2-部署到-firebase-deploy)
3. [常見問題 (FAQ)](#3-常見問題-faq)

---

## 1. 同步代碼 (Push & Pull)

### 📤 上傳代碼 (Push)
當你完成了新的功能或修復了 Bug，請按照以下步驟將代碼上傳到 GitHub：

```bash
# 1. 加入所有變更
git add .

# 2. 提交變更 (請將 "your message" 替換為實際的修改內容)
git commit -m "feat: your message here"

# 3. 推送到 GitHub
git push
```

### 📥 下載更新 (Pull)
當其他人在 GitHub 上更新了代碼，你需要將其下載到本地：

```bash
git pull
```

---

## 2. 部署到 Firebase (Deploy)

當你準備好將網站發布到線上環境 (Live Site) 時，請執行以下指令。

### 🚀 一鍵部署 (推薦)
我們建議使用以下組合指令，確保先構建 (Build) 再部署，避免發布舊的代碼。

```bash
# Windows (PowerShell) / Mac / Linux
npm run build && npx firebase-tools deploy
```

> **注意**：如果你的終端機不支援 `&&` (例如某些舊版 CMD)，請分開執行：
> 1. `npm run build`
> 2. `npx firebase-tools deploy`

### 步驟詳解
1.  **構建專案 (`npm run build`)**：
    這會將你的 React 代碼編譯並優化到 `dist` 資料夾中。這是發布前的必要步驟。

2.  **部署 (`npx firebase-tools deploy`)**：
    這會將 `dist` 資料夾中的內容上傳到 Firebase Hosting。
    *   我們使用 `npx firebase-tools` 而不是 `firebase`，以確保使用專案內安裝的正確版本，避免全域環境變數問題。

---

## 3. 常見問題 (FAQ)

### Q: 部署時出現 "firebase: command not found"？
**A:** 請使用 `npx firebase-tools deploy`。這會自動使用專案依賴中的 Firebase 工具，無需全域安裝。

### Q: 部署後線上看不到更新？
**A:**
1.  確認你有先執行 `npm run build`。
2.  瀏覽器可能有快取，請嘗試 **強制重新整理** (Ctrl + F5 或 Cmd + Shift + R)。

### Q: 什麼時候該寫 Changelog？
**A:** 建議在每次 **Deploy** 之前，更新 `src/pages/public/Changelog.jsx` 和 `CHANGELOG.md`，讓使用者知道這次更新了什麼。
